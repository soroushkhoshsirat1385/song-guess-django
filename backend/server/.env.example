# Postgres-first (recommended)
DB_NAME=songgame
DB_USER=postgres
DB_PASSWORD=yourpassword
DB_HOST=localhost
DB_PORT=5432

# Optional: SQLite quick-run (set to 1 to bypass Postgres locally)
USE_SQLITE=1
